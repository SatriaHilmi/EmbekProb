---
import deadpool from "../assets/deadpoll.jpg";
---

<!-- Wrapper utama -->
<div
  class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 rounded-xl overflow-hidden shadow-lg"
>
  <!-- Card 1 -->
  <div class="relative h-96 overflow-hidden group">
    <!-- Gambar penuh -->
    <img
      src={deadpool.src}
      alt="Farmer"
      class="w-full h-full object-cover transform transition-transform duration-500 ease-in-out group-hover:scale-110"
    />

    <!-- Overlay gradient bawah -->
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/40 to-transparent"
    >
    </div>

    <!-- Text + Button -->
    <div class="absolute bottom-0 w-full p-6 text-center">
      <h2 class="text-white font-bold text-3xl mb-4 font-bebas">
        kambing etawa premium
      </h2>
      <!-- <button
        class="px-6 py-2 bg-white text-green-900 font-semibold rounded-lg shadow-md cursor-pointer"
      >
        CHOOSE
      </button> -->
    </div>
  </div>

  <!-- Card 2 -->
  <div class="relative h-96 overflow-hidden group">
    <!-- Gambar penuh -->
    <img
      src={deadpool.src}
      alt="Farmer"
      class="w-full h-full object-cover transform transition-transform duration-500 ease-in-out group-hover:scale-110"
    />

    <!-- Overlay gradient bawah -->
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/40 to-transparent"
    >
    </div>

    <!-- Text + Button -->
    <div class="absolute bottom-0 w-full p-6 text-center">
      <h2 class="text-white font-bold text-3xl mb-4 font-bebas">
        breeding kambing etawa
      </h2>
      <!-- <button
        class="px-6 py-2 bg-white text-green-900 font-semibold rounded-lg shadow-md"
      >
        CHOOSE
      </button> -->
    </div>
  </div>

  <!-- Card 3 -->
  <div class="relative h-96 overflow-hidden group">
    <!-- Gambar penuh -->
    <img
      src={deadpool.src}
      alt="Farmer"
      class="w-full h-full object-cover transform transition-transform duration-500 ease-in-out group-hover:scale-110"
    />

    <!-- Overlay gradient bawah -->
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/40 to-transparent"
    >
    </div>

    <!-- Text + Button -->
    <div class="absolute bottom-0 w-full p-6 text-center">
      <h2 class="text-white font-bold text-3xl mb-4 font-bebas">
        susu kambing etawa
      </h2>
      <!-- <button
        class="px-6 py-2 bg-white text-green-900 font-semibold rounded-lg shadow-md"
      >
        CHOOSE
      </button> -->
    </div>
  </div>

  <!-- Card 4 -->
  <div class="relative h-96 overflow-hidden group">
    <!-- Gambar penuh -->
    <img
      src={deadpool.src}
      alt="Farmer"
      class="w-full h-full object-cover transform transition-transform duration-500 ease-in-out group-hover:scale-110"
    />

    <!-- Overlay gradient bawah -->
    <div
      class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/40 to-transparent"
    >
    </div>

    <!-- Text + Button -->
    <div class="absolute bottom-0 w-full p-6 text-center">
      <h2 class="text-white font-bold text-3xl mb-4 font-bebas">
        daging kambing
      </h2>
      <!-- <button
        class="px-6 py-2 bg-white text-green-900 font-semibold rounded-lg shadow-md"
      >
        CHOOSE
      </button> -->
    </div>
  </div>
</div>
